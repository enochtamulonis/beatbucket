<div class="flex flex-col items-center w-full">
  <%= link_to "Create new song", new_song_path, class: "bg-blue-500 max-w-md w-full p-3 rounded-full text-blue-50 text-center focus:ring-2 focus:ring-indigo-400" %>
</div>

<div class="mt-4 w-full">
  <div class="max-w-md w-full mx-auto grid gap-4">
    <%= render partial: "song", collection: @songs %>
  </div>
</div>