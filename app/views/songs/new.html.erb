<div class="max-w-xl w-full mx-auto">
<%= form_with model: @song do |form| %>
  <div class="grid">
    <%= form.label :title %>
    <%= form.text_field :title %>
    <br/>
    <%= form.label :audio %>
    <%= render "audio_form", form: form %>
  </div>

  <div class="mt-6">
    <%= form.submit "Create song", class: "bg-blue-500 text-blue-50 p-3 rounded-lg cursor-pointer" %>
  </div>
<% end %>